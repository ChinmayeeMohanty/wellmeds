spring.application.name=med-app
spring.mongodb.uri= ${MONGO_URI}
logging.level.org.springframework.data.mongocdb.core=debug
spring.ai.google.genai.api-key= ${GOOGLE_API_KEY}
spring.ai.google.genai.chat.model= gemini-3.1-flash-lite

#email notification
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username= ${MUSER_NAME}
spring.mail.password= ${PASS_KEY}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com
spring.mail.properties.mail.smtp.starttls.required=true

#razorpay
razorpay.key_id= ${RAZORPAY_KEY_ID}
razorpay.key_secret= ${RAZORPAY_KEY_SECRET}

#Intial Users
app.security.admin.username=${APP_SECURITY_ADMIN_USERNAME}
app.security.admin.password=${APP_SECURITY_ADMIN_PASSWORD}

app.security.patient.username=${APP_SECURITY_PATIENT_USERNAME}
app.security.patient.password=${APP_SECURITY_PATIENT_PASSWORD}

app.security.doctor.username=${APP_SECURITY_DOCTOR_USERNAME}
app.security.doctor.password=${APP_SECURITY_DOCTOR_PASSWORD}

app.security.pharmacist.username=${APP_SECURITY_PHARMACIST_USERNAME}
app.security.pharmacist.password=${APP_SECURITY_PHARMACIST_PASSWORD}